/**
 * App constants — Firebase-based auth.
 * Old OAuth server logic removed.
 */

export const SESSION_TOKEN_KEY = "app_session_token";
export const USER_INFO_KEY     = "app-runtime-user-info";

// Deep link scheme for the app
export const APP_SCHEME = "dakshproductivityecosystem";

// Firebase config is read directly from EXPO_PUBLIC_FIREBASE_* env vars in lib/firebase.ts
